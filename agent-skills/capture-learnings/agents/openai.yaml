interface:
  display_name: "Capture Learnings"
  short_description: "Сохранить предпочтения и знания после задачи"
  default_prompt: "Use $capture-learnings only to record a reusable, evidenced learning after explicit user approval."

policy:
  allow_implicit_invocation: false
