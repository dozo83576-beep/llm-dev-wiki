interface:
  display_name: "Site Deploy"
  short_description: "Релиз: env, миграции, rollback, monitoring"
  default_prompt: "Use $site-deploy to ship safely with rollback-first and monitoring."

policy:
  allow_implicit_invocation: false
