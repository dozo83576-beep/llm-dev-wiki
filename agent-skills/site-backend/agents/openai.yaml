interface:
  display_name: "Site Backend"
  short_description: "API, сервисы, БД и серверная безопасность"
  default_prompt: "Use $site-backend to implement APIs, services, and the server security boundary."

policy:
  allow_implicit_invocation: false
