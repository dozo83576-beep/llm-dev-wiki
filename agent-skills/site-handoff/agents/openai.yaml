interface:
  display_name: "Site Handoff"
  short_description: "Передача клиенту: handoff.md, доступы, приёмка, поддержка"
  default_prompt: "Use $site-handoff to deliver the site to the client: handoff doc, access transfer, sign-off."

policy:
  allow_implicit_invocation: false
