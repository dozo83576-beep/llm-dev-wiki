interface:
  display_name: "Build Modern Site"
  short_description: "Оркестратор сборки сайта от идеи до деплоя"
  default_prompt: "Use $build-modern-site, docs/01-development-process/site-pipeline-map.md and resources/site-pipeline-contract.json v2 to route a new site through all 17 phases by dependency graph. Run tools/new-site-preflight.ps1 first, record one primary playbook, delivery profile and supporting guides, verify phase evidence, then complete capture-learnings."

policy:
  allow_implicit_invocation: true
