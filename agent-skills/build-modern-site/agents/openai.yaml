interface:
  display_name: "Build Modern Site"
  short_description: "Маршрут direct или полный pipeline для сайта"
  default_prompt: "Use $build-modern-site to run preflight. Follow direct mode for a focused or simple static task; use contract v2 and only required phases for a complex full-pipeline project."

policy:
  allow_implicit_invocation: true
