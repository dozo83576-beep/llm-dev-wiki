interface:
  display_name: "Site Review"
  short_description: "Сводное ревью и security перед релизом"
  default_prompt: "Use $site-review to run the pre-release review and security checklists."

policy:
  allow_implicit_invocation: false
